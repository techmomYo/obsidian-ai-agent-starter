# Purpose: session cache — active task, pending actions, restored on each session start

---

Overwrite this file at the end of every session. Do not append.

```
date: [REPLACE: YYYY-MM-DD]
active-task: [REPLACE: one-line description of the current task]
last-decisions:
  - [REPLACE: decision]
pending-actions:
  - [REPLACE: action]
files-modified-this-session:
  - [REPLACE: path/to/file.md]
```
