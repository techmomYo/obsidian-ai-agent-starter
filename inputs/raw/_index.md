# Purpose: entry point for inputs/raw/ — web clips, research, open data

---

Unverified source material. Existing files are source data — add new files, never
edit or overwrite old ones without asking. Name files `topic-source-date.md` in lowercase with hyphens.

`sample-web-clip.md` shows the format — rename or delete it.
